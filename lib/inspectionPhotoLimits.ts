/** Plafond unique inspection terrain — Phase Photo Intelligence 2B. */
export const MAX_INSPECTION_PHOTOS = 500;

/** Alias explicite pour chargements galerie / QC / sélection IA. */
export const MAX_INSPECTION_PHOTOS_LOAD = MAX_INSPECTION_PHOTOS;

/** Phase 2B — alias stable pour clients/tests. */
export const MAX_PHOTOS_PER_INSPECTION = MAX_INSPECTION_PHOTOS;
