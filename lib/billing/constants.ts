/** Phase 7A — aucun blocage produit ; observation seule. */
export const BILLING_MONITOR_ONLY = true;

export const DEFAULT_TRIAL_DAYS = 14;

export const UPGRADE_RECOMMENDATION_THRESHOLD_PCT = 80;
